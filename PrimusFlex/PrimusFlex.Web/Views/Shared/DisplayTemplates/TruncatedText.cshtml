@model string

@if (Model != null)
{
    if (Model.Length > ViewBag.Length)
    {
        <text>@Model.Substring(0, ViewBag.Length)...</text>
    }
    else
    {
        <text>@Model</text>
    }
}
else
{
    <text></text>
}
